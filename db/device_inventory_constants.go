package db

// DeviceEphemeralBatchInventoryPrefix is the name prefix for short-lived inventories
// created for device actions (single/bulk). Must match api/projects/devices.go.
const DeviceEphemeralBatchInventoryPrefix = "windows_hosts batch "
