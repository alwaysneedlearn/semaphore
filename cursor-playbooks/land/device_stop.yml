---
- name: LAND 停止程序
  hosts: windows_hosts
  gather_facts: no
  max_fail_percentage: 100
  vars:
    sem_tasks_dir: "{{ playbook_dir }}/../shared/tasks"
    sem_files_dir: "{{ playbook_dir }}/../shared/files"
    sem_debug_tag: LAND
    _exe_name_default: LHBTS.exe
    _exe_name_env: "{{ (lookup('env', 'EXE_NAME') | default('', true) | trim) }}"
    _exe_name_base: "{{ (_exe_name_env if (_exe_name_env | length > 0) else _exe_name_default) | regex_replace('(?i)\\.exe$', '') }}"
    exe_name: "{{ _exe_name_base ~ '.exe' }}"
    process_name: "{{ ((lookup('env', 'PROCESS_NAME') | default('', true) | trim) | regex_replace('(?i)\\.exe$', '')) | default(_exe_name_base, true) }}"
    stop_graceful_process_name: "{{ ((lookup('env', 'STOP_GRACEFUL_PROCESS_NAME') | default('', true) | trim) | regex_replace('(?i)\\.exe$', '')) | default(process_name, true) }}"
    stop_popup_wait_seconds: "{{ ((lookup('env', 'STOP_POPUP_WAIT_SECONDS') | default('', true) | trim) | default('2', true)) | int }}"
    stop_popup_keyword: "{{ (lookup('env', 'STOP_POPUP_KEYWORD') | default('', true) | trim) | default('警告', true) }}"
    stop_force_after_graceful: "{{ ((lookup('env', 'STOP_FORCE_AFTER_GRACEFUL') | default('', true) | trim) | default('true', true)) | bool }}"
    stop_verify_process_name: "{{ (lookup('env', 'STOP_VERIFY_PROCESS_NAME') | default('', true) | trim) | default(stop_graceful_process_name, true) }}"
    land_api_scheme: "{{ (lookup('env', 'LAND_API_SCHEME') | default('', true) | trim) | default('http', true) }}"
    land_api_port: "{{ ((lookup('env', 'LAND_API_PORT') | default('', true) | trim) | default('8080', true)) | int }}"
    land_api_status_path: "{{ (lookup('env', 'LAND_API_STATUS_PATH') | default('', true) | trim) | default('/SyncLims/QueryStatus', true) }}"
    land_api_timeout: "{{ ((lookup('env', 'LAND_API_TIMEOUT') | default('', true) | trim) | default('8', true)) | int }}"
    land_api_token: "{{ (lookup('env', 'LAND_API_TOKEN') | default('', true) | trim) | default('landapi', true) }}"
    land_api_verify_tls: "{{ ((lookup('env', 'LAND_API_VERIFY_TLS') | default('', true) | trim) | default('true', true)) | bool }}"
    land_stop_check_api: "{{ ((lookup('env', 'LAND_STOP_CHECK_API') | default('', true) | trim) | default('false', true)) | bool }}"
    _devices_from_extra: "{{ devices | default([]) }}"
    _device_singleton: "{{ [device] if (device is defined) else [] }}"
    _semaphore_device_rows: "{{ _devices_from_extra if (_devices_from_extra | length > 0) else _device_singleton }}"

  tasks:
    - name: WinRM 建连（含重试）
      ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/winrm_ensure_reachable.yml"
    - name: WinRM 未连通则不再执行本主机后续步骤
      ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/winrm_gate_play_tasks.yml"
    - name: 解析当前设备信息
      ansible.builtin.set_fact:
        _land_dev: "{{ _semaphore_device_rows | selectattr('ip', 'equalto', inventory_hostname) | list | first | default(_semaphore_device_rows | selectattr('hostname', 'equalto', inventory_hostname) | list | first, true) | default({}) }}"
        _land_target_host: "{{ ((_semaphore_device_rows | selectattr('ip', 'equalto', inventory_hostname) | list | first | default(_semaphore_device_rows | selectattr('hostname', 'equalto', inventory_hostname) | list | first, true) | default({})).ip | default(inventory_hostname, true)) }}"

    - name: 部署 Windows 辅助脚本
      ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/deploy_sem_windows_helper_scripts.yml"

    - name: 优雅停止 LAND（主窗口 + 弹窗二次确认）
      ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/stop_program_close_main_window_confirm.yml"

    - name: 确认 LAND 进程已停止
      ansible.windows.win_shell: |
        $p = Get-Process -Name "{{ process_name }}" -ErrorAction SilentlyContinue | Select-Object -First 1
        if ($null -eq $p) { "NOT_RUNNING" } else { "RUNNING" }
      register: _land_process_after_stop
      changed_when: false

    - name: 停止后查询 LAND API 状态（可选）
      ansible.builtin.uri:
        url: "{{ land_api_scheme }}://{{ _land_target_host }}:{{ land_api_port }}{{ land_api_status_path }}"
        method: POST
        body_format: json
        body:
          token: "{{ land_api_token }}"
        timeout: "{{ land_api_timeout }}"
        validate_certs: "{{ land_api_verify_tls }}"
        return_content: true
        status_code: [200, 201, 204, 400, 401, 403, 404, 500, 502, 503]
      register: _land_api_after_stop
      delegate_to: localhost
      failed_when: false
      when: land_stop_check_api | bool

    - name: 登记 LAND 停止回调行
      ansible.builtin.set_fact:
        semaphore_callback_row:
          hostname: "{{ _land_dev.hostname | default('', true) }}"
          ip: "{{ _land_dev.ip | default(inventory_hostname, true) }}"
          device_status: unhealthy
          winrm_status: "{{ 'online' if (_winrm_session_ok | default(false) | bool) else 'offline' }}"
          api_status: offline
          abnormal_reason: >-
            {% set _proc = (_land_process_after_stop.stdout | default('') | trim) %}
            {% set _grace = (stop_graceful_result.stdout | default('') | trim) if (stop_graceful_result is defined) else '' %}
            {% if 'NOT_RUNNING' in _proc %}Stop completed; process not running
            {% elif 'STOP_TASK_NO_OUTPUT' in _grace %}Graceful stop produced no log (check scheduled task / helper on desktop); process still running
            {% elif 'POPUP_NOT_FOUND' in _grace %}Confirm popup not found (minimized/hidden window?); process still running
            {% elif 'STILL_RUNNING' in _grace or 'RUNNING' in _proc %}Stop failed: process still running
            {% else %}Stop failed: {{ _proc or _grace or 'unknown' }}{% endif %}

    - name: DEBUG LAND 停止结果
      ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/debug_sync_api_stop_snapshot.yml"

  post_tasks:
    - name: 写回 Semaphore 设备状态（post_tasks）
      run_once: true
      delegate_to: localhost
      block:
        - ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/semaphore_bulk_put_from_hostvars.yml"
- name: 写回 Semaphore 设备状态（独立 play）
  hosts: localhost
  gather_facts: false
  vars:
    sem_tasks_dir: "{{ playbook_dir }}/../shared/tasks"
    sem_files_dir: "{{ playbook_dir }}/../shared/files"
  tasks:
    - name: 执行 bulk 回调
      ansible.builtin.include_tasks: "{{ sem_tasks_dir }}/semaphore_bulk_put_from_hostvars.yml"