---
# Forward to shared TDengine publish (kept for NEWARE sem_profile_tasks_dir includes).
- ansible.builtin.include_tasks: "{{ playbook_dir }}/../shared/tasks/semaphore_tdengine_publish_from_bulk.yml"
