---
# 由 winrm_connect_one_attempt 在 attempt>1 时 include；meta 任务勿写 when（Ansible 不支持）
- name: 重置 WinRM 连接
  ansible.builtin.meta: reset_connection

- name: 等待重试间隔
  ansible.builtin.pause:
    seconds: "{{ winrm_connect_retry_delay | int }}"
