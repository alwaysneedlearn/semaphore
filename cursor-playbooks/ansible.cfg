[defaults]
host_key_checking = False
gathering = explicit
# Batch device plays use strategy: free. Raise forks so one hung host occupies
# only one worker; others keep running. Override with ANSIBLE_FORKS or --forks.
forks = 200
