---
# Deprecated: use land_api_modify_config_before_stop.yml + land_api_modify_config_after_start_retry.yml
# (ModifyConfig only while LAND is running; retry after start if the pre-stop attempt failed.)

- name: 程序运行中：API 修改配置（停止前）
  ansible.builtin.include_tasks: land_api_modify_config_before_stop.yml
